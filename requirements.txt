# Thesis project dependencies — env lock (2026-04-20)
# Anchor: Python 3.11 + torch 2.11.0.dev20260203+cu128 + NVIDIA RTX 5080
#
# INSTALL STRATEGY A (two-stage, default):
#   Stage 1 (separate command, nightly index):
#     pip install --pre torch==2.11.0.dev20260203+cu128 \
#       --index-url https://download.pytorch.org/whl/nightly/cu128
#   Stage 2 (this file, PyPI default):
#     pip install -r requirements.txt
#
# Torch NOT listed below on purpose — installed in Stage 1 via nightly index.
# If Strategy B (single-file --extra-index-url) is later chosen, prepend:
#   --extra-index-url https://download.pytorch.org/whl/nightly/cu128
#   --pre
#   torch==2.11.0.dev20260203+cu128

# --- Pinned (manifest anchor) ---
lightning==2.6.1
pytorch-forecasting==1.6.1
numpy==2.2.6

# --- Compatible ranges (manifest does not record exact; must be compatible with the pinned triad) ---
pandas>=2.2,<3
scikit-learn>=1.5,<2
shap>=0.45,<1
matplotlib>=3.8,<4

# --- v2 pipeline additions (2026-04-22) ---
pyyaml>=6          # src/common.py::load_config YAML parsing
arch>=7.0          # src/evaluation/post_training_analysis.py moving-block bootstrap
pytest>=8          # tests/ runner
ruff>=0.5          # linter (configured via pyproject.toml::[tool.ruff])
